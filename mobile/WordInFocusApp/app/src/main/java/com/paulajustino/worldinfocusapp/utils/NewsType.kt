package com.paulajustino.worldinfocusapp.utils

/**
 * Tipos de notícias disponíveis no aplicativo.
 *
 * @param type Tipo de notícia.
 */
enum class NewsType(val type: String) {
    RECENTS("recentes"),
    AGRO("agro")
}